// The final project's code should look something like:

use Renderer::Draw

#[derive()]
enum Arguments {
    i32,
    &str,
}

fn main() {
    let args: Vec<Arguments> = readline("How many objects?\n What is its position and velocity?").parse();
    runSim(args)
}

fn runSum(args: Vec<Arguments>) {
    add
}

